import type { LocalizationResource } from "@clerk/types"

export async function getClerkLocale(_lang: string): Promise<LocalizationResource | undefined> {
  return undefined
}

export async function getInitialClerkLocale(): Promise<LocalizationResource | undefined> {
  return undefined
}
